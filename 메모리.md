# LangChain 대화 메모리 7종

> 기준 환경: `langchain==1.4.1`, `langchain-classic==1.0.8`,  
> `langchain-community==0.4.2`

LangChain 1.x에서 아래 클래스들은 **legacy API**이므로 `langchain.memory`가 아닌
`langchain_classic.memory` 경로를 사용해야 한다. KG 메모리는 현재 환경이 안내하는
`langchain_community.memory.kg` 경로를 사용한다.

## 0. 준비

Jupyter Notebook에서 한 번만 실행한다.

```python
!pip install -U langchain langchain-classic langchain-community langchain-openai python-dotenv
```

`.env`

```text
OPENAI_API_KEY=your_api_key
```

공통 설정:

```python
from dotenv import load_dotenv
from langchain_openai import ChatOpenAI

load_dotenv()

# 토큰/엔티티/KG/요약 메모리에서 사용
llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)
```

`save_context()`는 대화를 저장하고, `load_memory_variables()`는 현재 입력에 맞는
메모리를 불러온다. 예제에서는 키를 명확하게 하기 위해
`input_key="input"`, `output_key="output"`을 사용한다.

---

## 1. ConversationBufferMemory

### 핵심 개념

- 전체 대화를 순서대로 모두 보관한다.
- 구현은 가장 간단하지만, 대화가 길어질수록 프롬프트와 토큰 사용량이 계속 늘어난다.
- 짧은 대화, 디버깅, 프로토타입에 적합하다.

### 공식 개발 문서

- [ConversationBufferMemory API Reference](https://reference.langchain.com/python/langchain-classic/memory/buffer/ConversationBufferMemory): 생성자 인자, 메서드, 반환값을 확인할 수 있는 legacy API 문서
- [LangChain Short-term memory](https://docs.langchain.com/oss/python/langchain/short-term-memory): 현재 권장되는 checkpointer 기반 대화 메모리와 긴 대화 관리 방법

```python
from langchain_classic.memory.buffer import ConversationBufferMemory

memory = ConversationBufferMemory(
    memory_key="history",
    input_key="input",
    output_key="output",
    return_messages=True,
)

memory.save_context(
    {"input": "제 이름은 민수입니다."},
    {"output": "반갑습니다, 민수님."},
)
memory.save_context(
    {"input": "저는 부산에 살아요."},
    {"output": "부산에 거주하시는군요."},
)

result = memory.load_memory_variables({"input": "내 정보를 알려줘."})
print(result["history"])
```

---

## 2. ConversationBufferWindowMemory

### 핵심 개념

- 가장 최근 `k`개의 **대화 turn**만 보관한다.
- 메모리 크기가 예측 가능하지만, 최근 k개의 대화만 기억하고, 그보다 오래된 대화는 삭제한다
- `k=2`면 최근 사용자-AI 쌍 2개를 남긴다.

```python
from langchain_classic.memory.buffer_window import ConversationBufferWindowMemory

memory = ConversationBufferWindowMemory(
    k=2,
    memory_key="history",
    input_key="input",
    output_key="output",
    return_messages=True,
)

for number in range(1, 4):
    memory.save_context(
        {"input": f"질문 {number}"},
        {"output": f"답변 {number}"},
    )

# '질문 1 / 답변 1'은 창 밖으로 밀려나고 최근 2 turn만 반환된다.
result = memory.load_memory_variables({"input": "현재 대화"})
print(result["history"])
```

---

## 3. ConversationTokenBufferMemory

### 핵심 개념

- 메시지 개수가 아니라 **토큰 수**를 기준으로 최근 대화를 남긴다.
- 저장 후 토큰 수가 `max_token_limit`를 넘으면 오래된 메시지부터 제거한다.
- 실제 모델의 토큰 계산을 위해 `llm`이 필요하다.

```python
from langchain_classic.memory.token_buffer import ConversationTokenBufferMemory

memory = ConversationTokenBufferMemory(
    llm=llm,
    max_token_limit=100,
    memory_key="history",
    input_key="input",
    output_key="output",
    return_messages=True,
)

memory.save_context(
    {"input": "서울 2박 3일 여행을 계획하고 있어요."},
    {"output": "궁궐, 남산, 성수동을 포함한 일정을 만들어 보겠습니다."},
)
memory.save_context(
    {"input": "매운 음식은 못 먹어요."},
    {"output": "식당을 선정할 때 맵지 않은 메뉴로 고르겠습니다."},
)

print(memory.load_memory_variables({"input": "일정을 보여줘."})["history"])
```

---

## 4. ConversationEntityMemory

### 핵심 개념

- 대화에서 사람, 장소, 조직 같은 **엔티티**를 추출하고 각 엔티티의 요약을 갱신한다.
- 개인화와 고객/프로젝트 정보 추적에 유용하다.
- 엔티티 추출과 요약에 LLM 호출이 발생한다.
- 현재 입력에서 엔티티를 알아내야 하므로, 일반적인 체인처럼 **load → 응답 생성 → save** 순서로 사용한다.

```python
from langchain_classic.memory.entity import ConversationEntityMemory

memory = ConversationEntityMemory(
    llm=llm,
    input_key="input",
    output_key="output",
    return_messages=False,
)

user_input = "Alice works at Acme and lives in Seoul."
memory.load_memory_variables({"input": user_input})  # 현재 엔티티 추출
memory.save_context(
    {"input": user_input},
    {"output": "I will remember that information about Alice."},
)

result = memory.load_memory_variables(
    {"input": "Where does Alice live?"}
)
print(result["entities"])
print(result["history"])
```

> 기본 엔티티 추출 프롬프트가 영어 고유명사에 맞춰져 있어, 이 최소 예제는
> 추출 결과를 안정적으로 보이기 위해 영문을 사용했다.

---

## 5. ConversationKGMemory

### 핵심 개념

- 대화의 사실을 **(주어, 관계, 목적어)** 트리플로 추출해 지식 그래프에 저장한다.
- 관계 중심의 질문과 여러 엔티티 사이의 연결을 추적하는 데 유용하다.
- 사실 추출에 LLM을 사용하며, 기본 그래프는 프로세스 메모리 내의 NetworkX 그래프다.

```python
# KG는 langchain_classic.memory.kg가 아닌 이 경로를 사용한다.
from langchain_community.memory.kg import ConversationKGMemory

memory = ConversationKGMemory(
    llm=llm,
    memory_key="history",
    input_key="input",
    output_key="output",
    return_messages=False,
)

memory.save_context(
    {"input": "Alice works at Acme. Acme is located in Seoul."},
    {"output": "Understood."},
)

result = memory.load_memory_variables({"input": "Tell me about Alice."})
print(result["history"])

# 저장된 트리플 확인
print(memory.kg.get_triples())
```

---

## 6. ConversationSummaryMemory

### 핵심 개념

- 전체 대화를 그대로 보관하지 않고, 새 대화가 추가될 때 누적 **요약**을 갱신한다.
- 긴 대화의 토큰을 줄일 수 있지만 요약 과정에서 세부 정보가 손실될 수 있다.
- 대화를 저장할 때 요약을 갱신하기 위한 LLM 호출이 발생한다.

```python
from langchain_classic.memory.summary import ConversationSummaryMemory

memory = ConversationSummaryMemory(
    llm=llm,
    memory_key="history",
    input_key="input",
    output_key="output",
    return_messages=False,
)

memory.save_context(
    {"input": "다음 달에 제주도로 3일간 여행할 거예요."},
    {"output": "제주도 3일 여행 계획을 도와드릴게요."},
)
memory.save_context(
    {"input": "렌터카 없이 버스로 다니고 싶어요."},
    {"output": "버스 이동을 기준으로 일정을 구성하겠습니다."},
)

print(memory.load_memory_variables({"input": "여행 계획을 요약해줘."})["history"])
```

---

## 7. VectorStoreRetrieverMemory

### 핵심 개념

- 대화를 임베딩해 벡터 저장소에 보관한 뒤, 현재 질문과 **의미적으로 관련된** 기억만 검색한다.
- 최근 대화가 아니라 오래된 대화에서도 관련 정보를 찾을 수 있다.
- 최소 예제는 외부 DB와 API 비용 없이 실행하도록 메모리 내 벡터 저장소와 테스트용 임벡딩을 사용한다.

```python
from langchain_classic.memory.vectorstore import VectorStoreRetrieverMemory
from langchain_core.embeddings import DeterministicFakeEmbedding
from langchain_core.vectorstores import InMemoryVectorStore

embeddings = DeterministicFakeEmbedding(size=1536)
vectorstore = InMemoryVectorStore(embedding=embeddings)
retriever = vectorstore.as_retriever(search_kwargs={"k": 2})

memory = VectorStoreRetrieverMemory(
    retriever=retriever,
    memory_key="history",
    input_key="input",
    return_docs=False,
)

memory.save_context(
    {"input": "제가 좋아하는 음료는 아이스 라떼예요."},
    {"output": "아이스 라떼를 좋아하시는군요."},
)
memory.save_context(
    {"input": "다음 휴가지는 제주도예요."},
    {"output": "제주도 여행을 준비하시는군요."},
)

result = memory.load_memory_variables(
    {"input": "내가 좋아하는 커피는 뭐였지?"}
)
print(result["history"])
```

> `DeterministicFakeEmbedding`은 학습과 테스트용이다. 실제 서비스에서는
> `OpenAIEmbeddings` 같은 의미 기반 임벡딩과 FAISS, Chroma 등의 영속적 벡터 DB를 사용한다.

---

## 한눈에 고르기

| 메모리 | 기준 | 장점 | 주의점 |
|---|---|---|---|
| `ConversationBufferMemory` | 전체 대화 | 구현이 간단하고 정보 손실이 없음 | 대화가 길어지면 토큰 증가 |
| `ConversationBufferWindowMemory` | 최근 k turn | 크기가 일정함 | 오래된 중요 정보 손실 |
| `ConversationTokenBufferMemory` | 최대 토큰 | 모델 컨텍스트 관리에 적합 | 오래된 메시지 제거 |
| `ConversationEntityMemory` | 엔티티별 요약 | 사람·장소·조직 추적 | LLM 호출 비용, 추출 오류 |
| `ConversationKGMemory` | 주어-관계-목적어 | 복잡한 관계 표현 | LLM 호출 비용, 트리플 추출 오류 |
| `ConversationSummaryMemory` | 누적 요약 | 긴 대화 압축 | 요약 시 세부 정보 손실 |
| `VectorStoreRetrieverMemory` | 의미 유사도 | 오래된 관련 기억 검색 | 임벡딩·벡터 DB 필요 |

## 중요 주의사항

- 이 7개 클래스는 LangChain 1.x의 신규 메모리 권장 방식이 아니라 `langchain-classic`에 남아 있는 legacy API다.
- 실행 시 `LangChainDeprecationWarning`이 표시될 수 있지만, 위 import 경로는 기준 환경에서 유효하다.
- 새 애플리케이션에서는 LangGraph의 checkpointer와 store를 이용한 short-term/long-term memory 구성을 우선 검토한다.
